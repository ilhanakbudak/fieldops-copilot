# Internal working notes — never published
.project/

# Environment & secrets
.env
.env.*
!.env.example
*.pem
secrets/
service-account*.json
*-[0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f][0-9a-f].json

# Node
node_modules/
.next/
out/
dist/
*.tsbuildinfo
next-env.d.ts

# Python
__pycache__/
*.py[cod]
.venv/
.mypy_cache/
.ruff_cache/
.pytest_cache/
*.egg-info/

# Runtime data, uploads and downloaded model weights.
# Anchored to the repository root on purpose: a bare `models/` also matched
# `services/api/app/db/models/` and quietly kept the ORM package out of the
# repository, so the published clone did not import.
/data/
/uploads/
/storage/
/models/
# Where the credential-free fallback actually puts its database. Named in full
# rather than as a bare `data/`, for the reason above.
/services/api/data/
*.db
*.sqlite3
# SQLite's write-ahead log and shared-memory files. `*.db` does not match them,
# which is how they reached a commit once.
*.db-shm
*.db-wal
*.sqlite3-shm
*.sqlite3-wal
.fastembed_cache/

# Coverage & logs
coverage/
htmlcov/
.coverage
logs/
*.log

# Regenerated by `next dev` on every run
apps/web/AGENTS.md
apps/web/CLAUDE.md

# Editor / OS
.vscode/
.idea/
.DS_Store
*.swp
